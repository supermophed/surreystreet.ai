# macOS
.DS_Store

# Claude Code session state
.claude/

# Local environment / secrets (defense-in-depth — this is a PUBLIC repo)
.env
.env.*
*.key
*.pem
credentials.json
credentials*.json
service-account*.json
secrets.*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Build artifacts / caches
__pycache__/
*.pyc
.pytest_cache/
node_modules/

# Backups / dumps / logs
*.bak
*.log
*.sql.dump

# Editorial working docs — never commit; the repo root is the public web root
editorial/

# Blog hero source art stays local — only the optimized .jpg ships
blog/*/*.png
